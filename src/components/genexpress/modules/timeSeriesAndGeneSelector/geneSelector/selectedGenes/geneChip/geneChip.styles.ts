import styled from 'styled-components';
import Chip from '@material-ui/core/Chip';

// eslint-disable-next-line import/prefer-default-export
export const GeneChipWrapper = styled(Chip)`
    margin: 6px 6px 0 0;
`;
