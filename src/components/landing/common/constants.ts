const SectionNames = {
    FEATURES: 'features',
    SCREENSHOTS: 'screenshots',
    CITING: 'citing',
    REFERENCES: 'references',
    CREDITS: 'credits',
    CONTACT: 'contact',
} as const;

export default SectionNames;
